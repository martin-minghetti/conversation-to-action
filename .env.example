# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Anthropic — powers the extraction pipeline
ANTHROPIC_API_KEY=

# Encryption key for BYOK credentials — generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=

# Discord bridge — shared secret between Discord bot and Vercel API
DISCORD_BRIDGE_SECRET=

# Cron — protects the /api/cron/process endpoint
CRON_SECRET=
